#!/bin/bash
# Disable tmux bell notifications for agy

STATE_DIR="${XDG_CONFIG_HOME:-$HOME/.config}/tmux"
mkdir -p "$STATE_DIR"
touch "$STATE_DIR/bell-notif-disabled"
echo "Tmux bell notifications disabled."
